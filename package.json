{
  "name": "aether-quant-ai-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd ui && npm install && npm run build",
    "vercel-build": "cd ui && npm install && npm run build"
  }
}
